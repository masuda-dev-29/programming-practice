学習内容

・grid() は行 (row) と列 (column) を使ってウィジェットを配置する。
・row・column の番号は 0から始まる。
・フォーム形式のGUIでは grid() が適している。
・同じ親ウィジェットでは pack() と grid() を混在させてはいけない。
・padx は左右の余白を設定する。
・pady は上下の余白を設定する。
・sticky はセル内での配置位置を指定し、フォームでは sticky="w" をよく使う。